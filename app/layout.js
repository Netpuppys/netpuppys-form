import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title:
    "Netpuppys || Book Your Appointment || Digital Marketing Agency in Gurgaon",
  description:
    "Book your appointment with Netpuppys, top Digital Marketing Agency in Gurgaon. Get expert strategies to grow your business with tailored marketing solutions. Contact us today!",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
